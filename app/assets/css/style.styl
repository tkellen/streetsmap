@import 'nib'
global-reset()

headerHeight = 45px
menuColor = #333

@import 'nav'
@import 'toggleswitch'

body
  overflow: hidden
  font-family: Lato, sans-serif
  font-size: 14px

.map
  margin-top: headerHeight