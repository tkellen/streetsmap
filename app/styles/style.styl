@import 'nib'
global-reset()

headerHeight = 45px
menuColor = #222

@import 'header'
@import 'sidebar'
@import 'toggleswitch'

body
  overflow: hidden
  font-family: Lato, sans-serif
  font-size: 16px

.map
  margin-top: headerHeight